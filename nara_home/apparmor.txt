#include <tunables/global>

profile nara_home flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  network inet stream,
  network inet6 stream,

  signal (receive) peer=unconfined,
  signal (send) set=(term,kill,int,hup,cont),

  /usr/local/bin/nara-home-launcher rix,
  /usr/bin/env ix,
  /usr/bin/python3 rix,
  /usr/bin/python3.13 rix,
  /opt/nara/bin/nara-home-mcp rix,
  /opt/nara/bin/python rix,
  /opt/nara/bin/python3 rix,
  /opt/nara/bin/python3.13 rix,
  /opt/nara/pyvenv.cfg r,
  /opt/nara/lib/python3.13/** r,
  /usr/local/share/nara-home/** r,
  /usr/lib/** rm,
  /lib/** rm,
  /lib64/** rm,
  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,

  /data/options.json r,
  /homeassistant_config/** r,
  deny /homeassistant_config/** wklx,

  owner /tmp/** rwk,
  /dev/null rw,
  /dev/urandom r,
}
